# Future Plans

- **Options Page:** Create a settings panel in the FreshRSS UI to allow users to configure:
  - Cache duration settings (the 16-day limit, 24-hour interval, 48-hour interval).
  - Thresholds for the emojis (e.g. what temperature counts as 'Viral' vs 'Hot').
  - Custom API endpoints for SearXNG or additional providers (like Mastodon).
