// Heatmap JS
document.addEventListener('DOMContentLoaded', () => {
    // 1. Find all elements with class 'heatmap-badge'
    // 2. Extract data-url
    // 3. Make AJAX request to ?c=heatmap&a=score&url=...
    // 4. Update the badge with the score and appropriate emoji
});
